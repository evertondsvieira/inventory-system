server:
  port: 8080

spring:
  kafka:
    bootstrap-servers: ${KAFKA+BROKER:localhost:9092}
    consumer:
      group-id: orchestrator-group
      auto-offset-reset:  latest

logging:
  level:
    org:
      apache:
        kafka: OFF